"""Application configuration.

Import the concrete module you need (``app.config.settings`` /
``app.config.logging``) rather than re-exporting here: the logging stack is
stdlib-only and must stay importable without pulling in the settings model.
"""
