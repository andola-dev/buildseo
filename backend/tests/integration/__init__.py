"""Integration tests: real PostgreSQL, real migrations, real SQLAlchemy.

Everything here needs a database. The suite is separated from ``tests/unit``
so the pure-logic tests stay runnable with no infrastructure at all.
"""
